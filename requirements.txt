# Core web stack
Django==5.2.17
djangorestframework==3.18.1
django-cors-headers==4.9.0
django-filter==26.1
psycopg2-binary==2.9.13
python-dotenv==1.2.3

# Auth
bcrypt==5.0.0
PyJWT==2.14.0

# HTTP client (TLE proxy)
requests==2.34.2

# Machine learning
numpy>=2.0,<3
pandas>=2.2,<3
scikit-learn>=1.5
scipy>=1.13
joblib>=1.4
xgboost>=2.1
matplotlib>=3.9

# MATLAB Engine for Python is NOT installed from here: it ships with MATLAB
# itself and its wheel path is machine-specific, so pinning it broke
# `pip install` on every machine without MATLAB at that exact path.
# It is optional -- the analytic Pc endpoints degrade gracefully without it.
# To enable them, install the engine that matches your MATLAB release:
#   pip install -r requirements-matlab.txt
